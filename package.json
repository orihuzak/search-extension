{
  "name": "app",
  "version": "0.2.0",
  "main": "view.ts",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/chrome": "^0.0.86",
    "@types/dom-inputevent": "^1.0.4",
    "ts-loader": "^6.0.4",
    "typescript": "^3.5.1",
    "webpack": "^4.39.2",
    "webpack-cli": "^3.3.7"
  },
  "dependencies": {
    "fuse.js": "^3.4.5"
  },
  "scripts": {
    "dev":  "webpack --watch --mode development --config webpack.config.js",
    "build": "webpack --mode production --config webpack.config.js",
    "zip": "zip -r search-tabs-bookmarks-history.zip dist/",
    "prepare": "yarn build && yarn zip"
  }
}
